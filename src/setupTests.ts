// src/setupTests.ts
// FIX: Use vitest-specific import for @testing-library/jest-dom to extend vitest's `expect` with custom matchers like .toBeInTheDocument().
import '@testing-library/jest-dom/vitest';
